#include<stdio.h>
main() {
 int n1, n2, n3, min;
 printf("정수 3개 입력 : ");
 scanf("%d %d %d", &n1, &n2, &n3);
 if (n1 < n2)
  if (n1 < n3)
   min = n1;
  else
   min = n3;
 else
  if (n2 < n3)
   min = n2;
  else
   min = n3;
 printf("최솟값은 %d이다\n", min);
}
